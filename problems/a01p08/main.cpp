// Include needed standard library functions
#include <utility>

// The main function is the entry point for any program written in C++
std::pair<size_t, size_t> noOddEvenBase10Digits() {
	auto const data = std::pair<size_t, size_t>{ 5, 5 };
	return data;
}
