#ifndef HEADER_H
#define HEADER_H

// Include needed standard library functions (STL)
#include <vector>
#include <string>

// The <signature> function  declaration
void forEachOne(std::vector<int>& data);
void forEachString(std::vector<std::string> const& strings);

#endif // HEADER_H
